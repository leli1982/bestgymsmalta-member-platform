name: Apply Batch 2 light UI v2

on:
  push:
    branches:
      - ui/member-app-light-refresh-batch2
    paths:
      - .github/workflows/batch2-light-ui-apply-v2.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ui/member-app-light-refresh-batch2
          fetch-depth: 0

      - name: Apply presentation-only changes
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace(text, old, new, label, count=None):
              found = text.count(old)
              if count is not None and found != count:
                  raise SystemExit(f"{label}: expected {count} occurrences, found {found}")
              if found == 0:
                  raise SystemExit(f"{label}: source pattern not found")
              return text.replace(old, new)

          passport_path = Path('components/passport/LivePassportPage.tsx')
          passport = passport_path.read_text()

          passport = replace(passport, '<div className="space-y-6">', '<div data-member-surface="passport-light" className="space-y-6 text-zinc-950">', 'passport root surfaces', 2)
          passport = replace(passport, 'min-h-[390px]', 'min-h-[330px]', 'passport signed-out hero', 1)
          passport = replace(passport, 'min-h-[340px]', 'min-h-[280px]', 'passport signed-out hero inner', 1)
          passport = replace(passport, 'min-h-[430px]', 'min-h-[350px]', 'passport hero', 1)
          passport = replace(passport, 'min-h-[380px]', 'min-h-[300px]', 'passport hero inner', 1)

          passport = replace(passport, 'rounded-2xl border border-white/10 bg-white/[0.04] p-4', 'rounded-2xl border border-zinc-200 bg-white p-4 shadow-sm', 'passport summary cards', 3)
          passport = replace(passport, 'mt-3 text-3xl font-black text-white', 'mt-3 text-3xl font-black text-zinc-950', 'passport summary values', 3)
          passport = replace(passport, 'text-white/35', 'text-zinc-500', 'passport summary labels', 3)

          passport = replace(passport, 'rounded-[2rem] border border-[#fcb415]/25 bg-[#fcb415]/10 p-5', 'rounded-[2rem] border border-orange-200 bg-orange-50 p-5 shadow-sm', 'passport scan card', 1)
          passport = replace(passport, 'mt-0.5 shrink-0 text-[#fcb415]', 'mt-0.5 shrink-0 text-[#ff5a0a]', 'passport scan icon', 1)
          passport = replace(passport, 'text-[10px] font-black uppercase tracking-[.25em] text-[#fcb415]">\n              Check In', 'text-[10px] font-black uppercase tracking-[.25em] text-[#ff5a0a]">\n              Check In', 'passport scan eyebrow', 1)
          passport = replace(passport, 'mt-1 text-2xl font-black text-white">\n              Scan at reception', 'mt-1 text-2xl font-black text-zinc-950">\n              Scan at reception', 'passport scan heading', 1)
          passport = replace(passport, 'mt-3 text-sm font-bold leading-6 text-white/60">\n              Scan the gym QR code', 'mt-3 text-sm font-bold leading-6 text-zinc-600">\n              Scan the gym QR code', 'passport scan copy', 1)
          passport = replace(passport, 'mt-5 flex items-center justify-center gap-2 rounded-full bg-[#fcb415] px-5 py-4 text-sm font-black text-black', 'mt-5 flex items-center justify-center gap-2 rounded-full bg-[#ff5a0a] px-5 py-4 text-sm font-black text-white shadow-lg shadow-orange-200', 'passport scan CTA', 1)

          passport = replace(passport, 'overflow-hidden rounded-[2rem] border border-white/10 bg-white/[0.04]">', 'overflow-hidden rounded-[2rem] border border-zinc-200 bg-zinc-950 shadow-sm">', 'passport next gym card', 1)

          passport = replace(passport, 'rounded-[2rem] border border-white/10 bg-white/[0.04] p-5', 'rounded-[2rem] border border-zinc-200 bg-white p-5 shadow-sm', 'passport light sections', 3)
          passport = replace(passport, 'rounded-[2rem] border border-white/10 bg-white/[0.04] p-6 text-center', 'rounded-[2rem] border border-zinc-200 bg-white p-6 text-center shadow-sm', 'passport empty stamps card', 1)
          passport = replace(passport, 'mt-1 text-2xl font-black text-white', 'mt-1 text-2xl font-black text-zinc-950', 'passport section headings', 3)
          passport = replace(passport, 'mt-4 text-3xl font-black text-white">\n            Start your passport', 'mt-4 text-3xl font-black text-zinc-950">\n            Start your passport', 'passport empty heading', 1)
          passport = replace(passport, 'mt-3 text-sm font-bold leading-6 text-white/50">\n            Your first QR check-in', 'mt-3 text-sm font-bold leading-6 text-zinc-500">\n            Your first QR check-in', 'passport empty copy', 1)

          passport = replace(passport, 'flex items-center gap-4 rounded-[1.5rem] border border-[#fcb415]/20 bg-black/25 p-4', 'flex items-center gap-4 rounded-[1.5rem] border border-zinc-200 bg-zinc-50 p-4', 'passport stamped gym rows', 1)
          passport = replace(passport, 'truncate text-lg font-black text-white', 'truncate text-lg font-black text-zinc-950', 'passport stamped gym titles', 1)
          passport = replace(passport, 'mt-1 text-xs font-bold text-white/45', 'mt-1 text-xs font-bold text-zinc-500', 'passport row meta', 2)
          passport = replace(passport, 'className="text-white/30"', 'className="text-zinc-400"', 'passport stamped chevron', 1)

          passport = replace(passport, 'overflow-hidden rounded-[1.5rem] border border-white/10 bg-black/25', 'overflow-hidden rounded-[1.5rem] border border-zinc-200 bg-white shadow-sm', 'passport unvisited cards', 1)
          passport = replace(passport, 'truncate text-sm font-black text-white', 'truncate text-sm font-black text-zinc-950', 'passport unvisited titles', 1)
          passport = replace(passport, 'mt-1 truncate text-xs font-bold text-white/40', 'mt-1 truncate text-xs font-bold text-zinc-500', 'passport unvisited labels', 1)

          passport = replace(passport, 'rounded-[2rem] border border-[#fcb415]/25 bg-[#fcb415]/10 p-6 text-center', 'rounded-[2rem] border border-orange-200 bg-orange-50 p-6 text-center shadow-sm', 'passport complete card', 1)
          passport = replace(passport, 'mt-4 text-3xl font-black text-white">\n            Passport complete', 'mt-4 text-3xl font-black text-zinc-950">\n            Passport complete', 'passport complete heading', 1)
          passport = replace(passport, 'mt-3 text-sm font-bold leading-6 text-white/60">\n            You have stamped every active', 'mt-3 text-sm font-bold leading-6 text-zinc-600">\n            You have stamped every active', 'passport complete copy', 1)

          passport = replace(passport, 'flex items-center gap-4 rounded-[1.5rem] border border-white/10 bg-black/25 p-4', 'flex items-center gap-4 rounded-[1.5rem] border border-zinc-200 bg-zinc-50 p-4', 'passport recent rows', 1)
          passport = replace(passport, 'truncate text-base font-black text-white', 'truncate text-base font-black text-zinc-950', 'passport recent titles', 1)
          passport = replace(passport, 'rounded-[1.5rem] border border-white/10 bg-black/25 p-5 text-center', 'rounded-[1.5rem] border border-zinc-200 bg-zinc-50 p-5 text-center', 'passport no checkins', 1)
          passport = replace(passport, 'mt-3 text-sm font-bold text-white/45', 'mt-3 text-sm font-bold text-zinc-500', 'passport no checkins copy', 1)

          story_path = Path('components/story/StoryCreator.tsx')
          story = story_path.read_text()

          story = replace(story, 'return (\n    <div className="space-y-6">', 'return (\n    <div data-member-surface="story-light" className="space-y-6 text-zinc-950">', 'story root surface', 1)
          story = replace(story, 'min-h-[300px]', 'min-h-[290px]', 'story hero', 1)
          story = replace(story, 'min-h-[250px]', 'min-h-[240px]', 'story hero inner', 1)

          story = replace(story, 'rounded-[2rem] border border-[#fcb415]/25 bg-black/50 p-4 shadow-2xl', 'rounded-[2rem] border border-zinc-200 bg-white p-4 shadow-sm', 'story preview workspace', 1)
          story = replace(story, 'flex items-center justify-center gap-2 rounded-full bg-[#fcb415] px-5 py-4 text-sm font-black text-black disabled:opacity-60', 'flex items-center justify-center gap-2 rounded-full bg-[#ff5a0a] px-5 py-4 text-sm font-black text-white shadow-lg shadow-orange-200 disabled:opacity-60', 'story share CTA', 1)
          story = replace(story, 'flex items-center justify-center gap-2 rounded-full border border-white/10 bg-white/[0.04] px-5 py-4 text-sm font-black text-white disabled:opacity-60', 'flex items-center justify-center gap-2 rounded-full border border-zinc-200 bg-zinc-50 px-5 py-4 text-sm font-black text-zinc-950 disabled:opacity-60', 'story download CTA', 1)
          story = replace(story, 'mt-4 text-center text-xs font-bold leading-5 text-white/35', 'mt-4 text-center text-xs font-bold leading-5 text-zinc-500', 'story workspace help', 1)

          story = replace(story, 'rounded-[2rem] border border-white/10 bg-white/[0.04] p-5', 'rounded-[2rem] border border-zinc-200 bg-white p-5 shadow-sm', 'story editor sections', 4)
          story = replace(story, 'mt-1 text-2xl font-black text-white', 'mt-1 text-2xl font-black text-zinc-950', 'story section headings', 4)

          story = replace(story, 'flex items-center justify-center gap-2 rounded-full bg-[#fcb415] px-5 py-4 text-sm font-black text-black', 'flex items-center justify-center gap-2 rounded-full bg-[#ff5a0a] px-5 py-4 text-sm font-black text-white', 'story take photo CTA', 1)
          story = replace(story, 'flex items-center justify-center gap-2 rounded-full border border-white/10 bg-white/[0.04] px-5 py-4 text-sm font-black text-white', 'flex items-center justify-center gap-2 rounded-full border border-zinc-200 bg-zinc-50 px-5 py-4 text-sm font-black text-zinc-950', 'story secondary photo controls', 2)

          story = replace(story, 'rounded-2xl border border-white/10 bg-black/25 p-3 text-xs font-black text-white/60 transition hover:border-[#fcb415]/50 hover:text-white', 'rounded-2xl border border-zinc-200 bg-zinc-50 p-3 text-xs font-black text-zinc-600 transition hover:border-[#ff5a0a]/50 hover:text-zinc-950', 'story sticker buttons', 1)
          story = replace(story, 'mt-5 rounded-[1.5rem] border border-white/10 bg-black/25 p-4', 'mt-5 rounded-[1.5rem] border border-zinc-200 bg-zinc-50 p-4', 'story selected item panel', 1)
          story = replace(story, 'text-xs font-black uppercase tracking-[.18em] text-white/35', 'text-xs font-black uppercase tracking-[.18em] text-zinc-500', 'story editor labels', 8)
          story = replace(story, 'rounded-full bg-[#fcb415] px-5 py-4 text-sm font-black text-black', 'rounded-full bg-[#ff5a0a] px-5 py-4 text-sm font-black text-white', 'story reset selected CTA', 1)
          story = replace(story, 'flex items-center justify-center gap-2 rounded-full border border-red-500/30 bg-red-500/10 px-5 py-4 text-sm font-black text-red-200', 'flex items-center justify-center gap-2 rounded-full border border-red-200 bg-red-50 px-5 py-4 text-sm font-black text-red-700', 'story remove selected CTA', 1)
          story = replace(story, 'mt-3 text-sm font-bold text-white/45', 'mt-3 text-sm font-bold text-zinc-500', 'story no selected copy', 1)

          story = replace(story, ': "border-white/10 bg-black/25"', ': "border-zinc-200 bg-zinc-50"', 'story no-template inactive state', 1)
          story = replace(story, 'className="text-lg font-black leading-tight text-white">\n                  No template', 'className="text-lg font-black leading-tight text-zinc-950">\n                  No template', 'story no-template title', 1)
          story = replace(story, 'className="mt-1 text-xs font-bold text-white/55">\n                  Photo + stickers only', 'className="mt-1 text-xs font-bold text-zinc-500">\n                  Photo + stickers only', 'story no-template copy', 1)

          story = replace(story, 'rounded-2xl border border-white/10 bg-black/30 px-4 py-3 text-sm font-bold text-white outline-none placeholder:text-white/25', 'rounded-2xl border border-zinc-200 bg-zinc-50 px-4 py-3 text-sm font-bold text-zinc-950 outline-none placeholder:text-zinc-400 focus:border-[#ff5a0a] focus:ring-2 focus:ring-orange-100', 'story text inputs', 2)
          story = replace(story, 'rounded-2xl border border-white/10 bg-black/30 px-4 py-3 text-sm font-bold leading-6 text-white outline-none placeholder:text-white/25', 'rounded-2xl border border-zinc-200 bg-zinc-50 px-4 py-3 text-sm font-bold leading-6 text-zinc-950 outline-none placeholder:text-zinc-400 focus:border-[#ff5a0a] focus:ring-2 focus:ring-orange-100', 'story textarea', 1)

          passport_path.write_text(passport)
          story_path.write_text(story)
          PY

      - name: Commit UI changes and remove runners
        shell: bash
        run: |
          rm .github/workflows/batch2-light-ui-apply.yml
          rm .github/workflows/batch2-light-ui-apply-v2.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add components/passport/LivePassportPage.tsx components/story/StoryCreator.tsx .github/workflows/batch2-light-ui-apply.yml .github/workflows/batch2-light-ui-apply-v2.yml
          git commit -m "ui: apply Batch 2 light Passport and Story Creator surfaces"
          git push origin HEAD:ui/member-app-light-refresh-batch2
